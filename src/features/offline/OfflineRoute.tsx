export function OfflineRoute() {
  return (
    <main aria-labelledby="offline-title">
      <p>Offline</p>
      <h1 id="offline-title">You are offline</h1>
      <p>
        Pacto cannot load this page without a connection. Reconnect and try
        again; private account data is never stored in the offline cache.
      </p>
    </main>
  );
}
